"""
Solution template for 009 — Product of Array Except Self.

Fill in the function body below, then run:
    pytest test_solution.py -v
"""


def product_except_self(nums: list[int]) -> list[int]:
    """
    Return an array where each element is the product of all other elements in nums.

    Must run in O(n) time without using division.

    Args:
        nums (list[int]): Input array of integers.

    Returns:
        list[int]: Output array where output[i] = product of nums except nums[i].
    """
    pass
