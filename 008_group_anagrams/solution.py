"""
Solution template for 008 — Group Anagrams.

Fill in the function body below, then run:
    pytest test_solution.py -v
"""


def group_anagrams(strs: list[str]) -> list[list[str]]:
    """
    Group strings that are anagrams of one another into sublists.

    Args:
        strs (list[str]): List of lowercase strings.

    Returns:
        list[list[str]]: Groups of anagrams. Order of groups and order within
                         each group do not matter.
    """
    pass
