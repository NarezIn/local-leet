"""
Solution template for 007 — 3Sum.

Fill in the function body below, then run:
    pytest test_solution.py -v
"""


def three_sum(nums: list[int]) -> list[list[int]]:
    """
    Find all unique triplets in nums that sum to zero.

    Args:
        nums (list[int]): Array of integers (length >= 3).

    Returns:
        list[list[int]]: List of unique triplets [a, b, c] where a + b + c == 0.
                         Each triplet should be sorted in non-decreasing order.
                         The outer list may be in any order.
    """
    pass
