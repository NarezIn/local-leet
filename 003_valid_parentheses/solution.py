"""
Solution template for 003 — Valid Parentheses.

Fill in the function body below, then run:
    pytest test_solution.py -v
"""


def is_valid(s: str) -> bool:
    """
    Determine whether a string of bracket characters is correctly matched and nested.

    Args:
        s (str): A string containing only '(', ')', '{', '}', '[', ']'.

    Returns:
        bool: True if every bracket is properly matched and nested, False otherwise.
    """
    pass
