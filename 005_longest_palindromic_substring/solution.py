"""
Solution template for 005 — Longest Palindromic Substring.

Fill in the function body below, then run:
    pytest test_solution.py -v
"""


def longest_palindrome(s: str) -> str:
    """
    Find the longest substring of s that is a palindrome.

    Args:
        s (str): The input string (length >= 1).

    Returns:
        str: The longest palindromic substring. If there are ties, any valid answer is accepted.
    """
    pass
